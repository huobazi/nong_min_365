<!DOCTYPE html>
<%= html_tag :class => "no-js " +  controller_name + "-controller" , :lang => "zh-CN" %>
<%= render "head" %>
<%= render_body_tag %>
<%= render "top_navbar" %>
<div class="container">
  <div id="error_page">
    <%= content_for?(:content) ? yield(:content) : yield %>
  </div>
</div>
<%= render "footer" %>
<%= render "javascripts" %><%# Javascript at the bottom for fast page loading %>
  </body>
</html>

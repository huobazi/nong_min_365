<!DOCTYPE html>
<%= html_tag :class => "no-js ", :lang => "zh-CN" %>
<%= render "head" %>
<%= render_body_tag %>
<%= render "top_navbar" %>
<div class='container <%="#{action_name}-action"%>'>
  <%= render "header" %>
  <div itemscope itemtype="http://data-vocabulary.org/Breadcrumb">
    <%= render_breadcrumb %>
  </div>
  <%= content_for?(:content) ? yield(:content) : yield %>
</div>
<%= render "footer" %>
<%= render "javascripts" %><%# Javascript at the bottom for fast page loading %>
  </body>
</html>

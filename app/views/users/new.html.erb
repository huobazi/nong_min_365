<% content_for :sitemap do %><span class="current">加入NongMin365.COM</span><% end %> <div class="content">
  <div class="box">
    <%= simple_form_for @user, :url => users_path do |f| %>
      <fieldset>
        <legend>注册</legend>
        <%= f.input :username, :hint => "user name xxx" %>
        <%= f.input :password %>
        <%= f.input :password_confirmation %>
        <div class="form-actions">
          <%= f.submit "提交注册信息", :class => "btn btn-primary", 'data-disable-with' => t("common.submitting") %>
        </div>
      </fieldset>
    <% end %>
  </div>
</div>

